import { defineConfig } from "vite";
import vue from "@vitejs/plugin-vue";

// 开发环境把 `/api` 和 `/health` 代理到 Python 后端。
export default defineConfig({
  plugins: [vue()],
  server: {
    host: "127.0.0.1",
    port: 5173,
    proxy: {
      "/api": {
        target: "http://127.0.0.1:8000",
        changeOrigin: true,
      },
      "/health": {
        target: "http://127.0.0.1:8000",
        changeOrigin: true,
      },
    },
  },
});
